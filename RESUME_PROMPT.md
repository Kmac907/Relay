Resume the Relay work from branch `fix/restore-throughput` at commit `508bc3c` or its latest descendant. Read `CONTINUATION.md` completely and treat its Authority section as the acceptance contract. Inspect the full branch diff before editing, then continue through implementation, tests, documentation, PR, and merge. Do not resume the target campaign until the Relay fix is merged.
